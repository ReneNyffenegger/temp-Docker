Docker compose is for orchestrating multiple containers

Docker compose file is a way for you to declaratively orchestrate the startup
of multiple containers, rather than run each Dockerfile separately with a bash
script, which would be much slower to write and harder to debug.
